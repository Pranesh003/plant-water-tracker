server:
  port: 8081

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: plant-care-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/**
